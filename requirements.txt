# pip fallback — the authoritative, fully-resolved environment is uv.lock (run `uv sync`).
# torch CPU build comes from PyTorch's index, not PyPI:
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.4.0
numpy==1.26.4
pandas==2.2.2
scipy==1.17.1
scikit-learn==1.4.2
matplotlib==3.9.2
seaborn==0.13.2
xgboost==3.0.2
lightgbm==4.3.0
joblib==1.5.2
tqdm==4.67.1
